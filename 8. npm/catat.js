// ! apa itu npm?

// ? node package manager adalah package manager untuk node js dimana kita bisa menginstall package yang dibuat oleh orang lain atau kita sendiri. npm ini sudah terinstall ketika kita menginstall node.js

// * npm bukan hanya node package manager 

// ? digit dalam version (1.0.0)
// * digit terakhir (ketiga) adalah patch version
// * digit kedua adalah perubahan minor
// * digit pertama adalah perubahan major

